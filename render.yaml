services:
  - type: web
    name: dearsunshine
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: DATA_DIR
        value: /data
      - key: OPENAI_API_KEY
        sync: false
